/**
 * 
 * @param date 日期时间，可以是日期对象、时间戳（毫秒）、日期格式化的表示方式
 * @param formater 格式化，如 'YYYY-MM-DD'
 */

export function formatDatetime(date: Date | number | string, formater: string): string {
  // TODO 
  return ''
}